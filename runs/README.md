# Runs

Training, evaluation, and intermediate CSV/JSON files are written here.

This directory is intentionally ignored by git because the generated files can be large.
Run the scripts from the repository root to recreate the files needed by the figures.
